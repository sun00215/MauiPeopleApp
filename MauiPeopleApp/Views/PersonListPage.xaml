<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="MauiPeopleApp.Views.PersonListPage"
             Title="People">
    
    <ContentPage.ToolbarItems>
        <ToolbarItem Text="Logout" Order="Primary" Priority="0" Clicked="OnLogoutClicked" />
    </ContentPage.ToolbarItems>

    <Grid RowDefinitions="Auto,*">
        
        <!-- Header Section -->
        <VerticalStackLayout Grid.Row="0" Padding="20,10" BackgroundColor="{DynamicResource Primary}">
            <Label Text="🔐 Authenticated Access" 
                   FontSize="16" 
                   TextColor="White" 
                   HorizontalOptions="Center" />
        </VerticalStackLayout>

        <!-- People List -->
        <CollectionView Grid.Row="1" 
                        ItemsSource="{Binding People}" 
                        SelectionMode="Single" 
                        SelectionChanged="OnSelectionChanged">
            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <Grid Padding="15" ColumnDefinitions="60,*,Auto" RowDefinitions="Auto">
                        <Frame Grid.Column="0" CornerRadius="25" WidthRequest="50" HeightRequest="50" Padding="0" IsClippedToBounds="True">
                            <Image Source="{Binding Avatar}" Aspect="AspectFill" />
                        </Frame>
                        <VerticalStackLayout Grid.Column="1" Margin="15,0" VerticalOptions="Center">
                            <Label Text="{Binding FullName}" FontAttributes="Bold" FontSize="16" />
                            <Label Text="{Binding Email}" FontSize="14" TextColor="Gray" />
                        </VerticalStackLayout>
                        <Label Grid.Column="2" Text=">" FontSize="18" TextColor="Gray" VerticalOptions="Center" />
                    </Grid>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>

    </Grid>

</ContentPage>